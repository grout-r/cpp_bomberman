#include	"GameEngine.hh"
/*
int			main()
{
  GameEngine		engine;

  if (engine.initialize() == false)
    return (-1);
  while (engine.update() != false)
    {
      engine.draw();

      sleep(5);
      exit(0);
    }
  engine.gameOver();
  return  (0);
  }*/
